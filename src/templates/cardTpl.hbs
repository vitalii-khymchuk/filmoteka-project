{{#each this}}
<li class="film-card" data-id={{id}}>
    <a class="film-card__link" href="">
        <img class="film-card__img" src={{backdrop_path}} alt='{{title}}' loading="lazy"/>
        <div class="film-card__info">
                <span class="film-card__title">{{title}}</span> <br>
                <span class="film-card__genre">{{genre_ids}}</span>
                <span class="film-card__year">{{release_date}}</span>
                <span class="film-card__rating">{{vote_average}}</span>
            </br>
              
        </div>
    </a>
    </li>

 {{/each}}